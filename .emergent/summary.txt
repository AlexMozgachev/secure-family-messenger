<analysis>
The AI engineer's work trajectory outlines an iterative development process for the Secure Messenger Builder application. Initially, the focus was on consolidating UI elements in the admin panel, specifically merging security features into a new  page and updating navigation in . This was followed by implementing system monitoring, and a user-specific device list. A significant portion of the work then shifted to resolving complex installation and compatibility issues across Ubuntu 20.04 and 22.04 LTS, requiring the creation of multiple installation and fix scripts, and adjusting frontend/backend package versions. Concurrently, the project evolved to remove the web installer, directly presenting the admin login, and moving SSL certificate installation to the admin panel. The previous engineer was deeply involved in troubleshooting persistent installation failures and frontend build issues.
</analysis>

<product_requirements>
The overarching goal is to develop a Secure Messenger Builder with a custom E2EE backend (FastAPI/MongoDB) and a React-based admin web panel. Core features include automated server setup, admin login/password management, comprehensive user CRUD (create/block/delete/reset password), security features (IP blocking, login attempt tracking, device monitoring with optional GPS), media policy, and QR profile generation. Recent feature requests and implementations include RU/EN localization, user role management, consolidated server settings (backup/restore, domain/IP management, SSL certificates) into a  page, and aggregate data display for Chats and Messages. The user also explicitly requested a system monitoring section (CPU, RAM, traffic) and a user-specific device list accessible via a modal from the user management section. A major recent change was the removal of the web installer, making the application directly present the admin login, with SSL installation moved to the admin panel.
</product_requirements>

<key_technical_concepts>
- **FastAPI**: Backend API development.
- **React**: Frontend UI development.
- **MongoDB**: Database for backend data storage, using .
- **E2EE (Custom)**: End-to-End Encryption implementation.
- **WebSockets**: Real-time communication.
- **JWT**: Authentication.
- **Shadcn UI & Tailwind CSS**: Frontend styling.
- ****: Internationalization (RU/EN).
- ****: Python library for system monitoring.
- ****: For service management on Linux.
</key_technical_concepts>

<code_architecture>


-   : The core FastAPI backend. It handles authentication, user management, security (IP blocking, login attempts), device monitoring, server settings, and WebSocket communication.
    -   **Changes**: Added  imports, new  endpoint for system stats, and  endpoint for SSL certificate installation.
-   : Main React component for routing.
-   : Admin panel entry.
    -   **Changes**: Replaced Security navigation with Settings. Integrated  component. Added state and functions for displaying a user-specific devices modal, including a Monitor button for each user.
-   : Consolidated server settings.
    -   **Changes**: Integrated content from . Added UI for Install SSL button, and associated  function.
-   : Old security page, now integrated into .
    -   **Changes**: Readability fix for Постоянно text in the Blocked IPs table. Removed the List of Devices button.
-   : New component for displaying CPU, RAM, disk, network usage, and uptime.
    -   **Changes**: Removed the List of Devices button.
-   : Contains translation strings.
    -   **Changes**: Added translations for system monitoring, device list, and SSL installation.
-   : New simplified installation script for Ubuntu 22.04, replacing older, problematic ones. It removes SSL installation from the initial setup and focuses on IPv4 configuration.
-   : A script for fixing IPv6/SSL issues in existing installations.
-    &  / : These files were created/updated to define compatible backend (Python) and frontend (Node.js) dependencies specifically for Ubuntu 22.04 LTS environments, resolving numerous installation conflicts.
</code_architecture>

<pending_tasks>
- Complete full review and debugging of the  script for full compatibility and error-free installation on a clean Ubuntu 22.04 LTS.
- Test the new SSL installation functionality within the admin panel (frontend and backend integration).
- Ensure that the web installer is completely removed/bypassed and that the application always directs to the admin login on fresh installations.
</pending_tasks>

<current_work>
The AI engineer is currently in a deep troubleshooting phase focused on the  script. The goal is to ensure a smooth and error-free installation of the application on a clean Ubuntu 22.04 LTS environment. This involves:
1.  **Removing Ubuntu 20.04 related artifacts:** All old installation scripts, , and  files tailored for Ubuntu 20.04 have been deprecated or removed.
2.  **Simplifying installation:** The  script was created, removing SSL certificate installation from the initial setup (to be handled via the admin panel later) and configuring primarily for IPv4 to avoid previous IPv6 issues.
3.  **Frontend/Backend compatibility:** The engineer created  and  to specify compatible Python and Node.js dependencies for Ubuntu 22.04, addressing previous conflicts (e.g.,  and  versions).  was re-added after being mistakenly removed, which caused a  error.
4.  **SSL management in admin panel:** New UI () and API () endpoints have been added for installing SSL certificates directly from the admin interface.

The immediate problem is that the  script is still not leading to a fully functional installation, and the user is reporting errors, including  in the frontend. The engineer is analyzing the script and its compatibility.
</current_work>

<optional_next_step>
Conduct a full, detailed analysis of  to identify and fix remaining critical errors and ensure full compatibility with Ubuntu 22.04 LTS.
</optional_next_step>
